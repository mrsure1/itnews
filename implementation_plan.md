# [Project] AI 기반 IT 뉴스 자동 큐레이션 서비스 구축 계획서

본 문서는 전문적인 개발 지식이 없는 비전공자도 AI를 활용하여 자신만의 'IT 뉴스 요약 서비스'를 만들 수 있도록 돕기 위한 상세 가이드라인입니다.

## 1. 프로젝트 개요 및 목적

이 프로젝트는 매일 쏟아지는 방대한 IT 뉴스를 일일이 찾아보는 번거로움을 해결하기 위해 탄생했습니다.
- **자동화**: 사람이 개입하지 않아도 정해진 시간마다 새로운 뉴스를 가져옵니다.
- **핵심 요약**: AI(Gemini)가 긴 기사를 3줄로 요약하여 바쁜 현대인에게 최적화된 정보를 제공합니다.
- **프리미엄 UI**: 깔끔하고 현대적인 디자인의 웹 페이지를 통해 기사를 카드 형태로 노출합니다.

---

## 2. 시스템 아키텍처 (동작 원리)

전체적인 흐름은 다음과 같습니다. 데이터가 어떻게 이동하는지 시각화한 그림입니다.

```text
[ 데이터 수집 ] ----> [ AI 처리/요약 ] ----> [ 데이터 저장 ] ----> [ 웹사이트 표시 ]
      |                   |                   |                   |
 (Naver 뉴스/RSS)      (Google Gemini)      (news_data.js)      (index.html)
      |                   |                   |                   |
 [ 파이썬 스크립트 ] <---------------- GitHub Actions (자동 실행기) ---------------->
```

---

## 3. 상세 구현 단계

### 단계 1: 환경 설정 및 준비물
비전공자가 가장 먼저 준비해야 할 요소들입니다.

1.  **Python 설치**: 뉴스 수집기를 실행하기 위한 기본 도구입니다.
2.  **API 키 발급**: Google AI Studio에서 Gemini API 키를 발급받아야 합니다. (무료 사용 가능 범위 확인)
3.  **GitHub 저장소**: 코드를 저장하고 웹사이트를 무료로 호스팅(포털처럼 띄우기)할 공간입니다.

### 단계 2: 뉴스 데이터 수집 (Scraper 구현)
웹 사이트(네이버 뉴스 등)에서 기사 제목과 링크를 가져오는 단계입니다.
- **주요 파일**: `analyze_naver_page.py`, `collect_news.py`
- **핵심 기술**: Python의 `BeautifulSoup` 라이브러리를 사용하여 뉴스 사이트의 HTML 구조를 분석하고 정보를 추출합니다.
- **비전공자 포인트**: "장바구니에 담기"와 비슷합니다. 필요한 정보(제목, 링크, 언론사)만 골라 담는 과정입니다.

### 단계 3: AI를 활용한 뉴스 요약 및 분류
가져온 기사를 똑똑하게 가공하는 단계입니다.
- **Gemini 연동**: 수집된 기사 본문을 Gemini AI에게 전달하고, "3줄로 요약해줘"라고 요청합니다.
- **카테고리 분류**: 기사 내용을 분석하여 'OpenAI', 'Google', 'NVIDIA', 'Tesla' 등 주제별로 자동 분류합니다.
- **이미지 프롬프트**: 기사 내용에 어울리는 이미지를 나중에 생성할 수 있도록 그림 설명(프롬프트)도 함께 생성합니다.

### 단계 4: 데이터 구조화 및 변환
방대한 데이터를 웹사이트가 이해할 수 있는 언어로 바꾸는 단계입니다.
- **JSON 저장**: 가공된 데이터를 `news_data.json` 형식으로 저장합니다.
- **JS 변환**: 웹 브라우저가 바로 읽을 수 있도록 `convert_json_to_js.py`를 통해 자바스크립트 파일로 변환합니다.

### 단계 5: 프리미엄 UI 디자인 (Frontend)
사용자가 직접 보게 될 화면을 만드는 단계입니다.
- **기술**: HTML5 + Vanilla JS + **Tailwind CSS**.
- **디자인 컨셉**:
    - **다크 모드 지원**: 눈이 편안한 어두운 배경과 세련된 색상 조합.
    - **카드 레이아웃**: 각 뉴스 요약을 깔끔한 카드(Card) 형태로 배치.
    - **애니메이션**: 마우스 커서를 올렸을 때의 부드러운 확대 효과(Hover effect).
- **반응형 웹**: 스마트폰과 PC 어디서든 최적화된 모습으로 보이도록 설계합니다.

### 단계 6: 자동화 배포 (GitHub Actions)
이 모든 과정을 24시간 자동으로 돌아가게 만드는 마법 같은 단계입니다.
- **스케줄 설정**: 매일 아침 또는 1시간마다 코드가 알아서 실행되도록 설정합니다.
- **GitHub Pages**: 코드가 업데이트되면 웹사이트 주소(https://아이디.github.io/itnews)에 자동으로 반영됩니다.

---

## 4. 필수 체크 리스트

- [ ] Python 3.x 버전 이상 설치
- [ ] google-generativeai 라이브러리 설치
- [ ] `.env` 파일에 GEMINI_API_KEY 설정
- [ ] index.html의 Tailwind CSS CDN 링크 확인

---

## 5. 향후 확장 아이디어 (Open Questions)

- **이미지 생성 자동화**: AI 요약문으로 실제 이미지를 생성하여 카드에 넣을까요?
- **푸시 알림**: 중요한 뉴스가 뜨면 이메일이나 메신저로 알림을 보낼까요?
- **다국어 지원**: 해외 IT 사이트(The Verge 등) 추가 수집을 원하시나요?
